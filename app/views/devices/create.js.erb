
$('#new_device').parent().remove();
$('#devices tbody').append("<%= escape_javascript render @device %>");
$('#devices tbody').append("<%= escape_javascript render "form", object: @user.devices.build, button_name: "Add" %>");

